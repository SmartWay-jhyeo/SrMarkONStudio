# HANDOFF — MarkON Studio

> 2026-08-20 시점 (`aabd6c1`). 다음 사람(또는 다음 세션의 나)이 읽고 바로 이어서 일할 수 있게.
> `CLAUDE.md`가 **규칙**이라면 이 문서는 **지금 상태**다. 규칙은 그쪽을 본다.

---

## 1. 한 줄 요약

STM32 v2.0 보드가 **USB로 GUI에 붙어 설정되고, 빼면 저장된 대로 혼자 돈다.**
그리고 이제 **PC 없이 보드 앞에서 읽을 수 있다** — J25 의 3.5인치 LCD 가 센서값을
띄운다.

**2026-08-19 에 실기기로 확인된 것**(전부 §2에 근거가 있다):

- **유압 센서**(J3, 0~250 bar) · **적외 온도**(J12 MLX90614) · **온습도**(J13 AM2320)
  가 동시에 읽힌다. 서로 다른 원리로 잰 두 온도가 0.05 °C 안에서 일치했다
- **옵토 디지털 입력**(J20) 이 90회 변화를 하나도 안 놓치고 잡았다 — 데이터시트
  §5.7 이 "출력"이라고 단정한 것이 **틀렸음이 실물로 확정**됐다
- **GNSS 양방향 통신** — 보드가 UM981 에 명령을 보내고 응답을 받는다. UTC 날짜·시각
  수신. **PPS 를 1 Hz 로 정확히 캡처**한다(짝지을 fix 가 실내라 아직 없을 뿐)
- **LCD** 가 색·방향·부분 갱신까지 맞다. 🔴 **화면을 그리는 동안에도 수집이 안 밀렸다**
  — `ain` 간격 중앙 100 ms / 최대 104 ms, 큐 `drops` 0

**2026-08-20 에 더한 것** — 목표는 **채널당 10 ms 수집**(카메라 동기, 카메라가 최대 33 ms):

- **시스템 클럭을 25 MHz 크리스털로 옮겼다**(HSE→PLL→정확히 64.000 MHz). 실기기
  레지스터로 확인(`RCC_CFGR=0x1b`, SWS=PLL1). 🔴 그 전까지는 **내부 RC** 로 돌았고
  ±1 %(1초당 최대 10 ms)라 **목표 분해능 전체만큼 틀릴 수 있었다.** 주파수를 그대로
  둔 덕에 I2C·SPI·TIM·UART 상수가 하나도 안 바뀌었다
- **수집이 슈퍼루프에서 떨어졌다** — TIM7(1 kHz)이 상태머신을 깨우고, SPI 완료 ISR 이
  다음 채널로 곧바로 잇는다. 🔴 그 전까지 다음 채널을 시작하는 것은 슈퍼루프뿐이었고
  **한 바퀴가 최악 194 ms** 였다(§5). 못 뜬 표본은 `drops` 에도 안 잡힌 채 사라졌다

### 🔴 10 ms 목표 달성 — 실기기 확인 (2026-08-20)

**7채널 × 10 ms 가 실물에서 돈다.** 30초에 채널마다 3,000개, 큐가 표본을 하나도 안 버린다.

| | 60 SPS / 100 ms | 2000 SPS / 10 ms, 7채널, 2 Mbps |
|---|---|---|
| 채널별 고유 표본(30초) | 200 (20초) | **2,998 ~ 3,001 / 3,000** |
| 획득 간격 | 중앙 100 / 최대 100 ms | **중앙 10 / 최대 10~20 ms** |
| 큐 | depth 0, peak 2, drops 0 | depth 0, **peak 4, drops 0** |
| 총 처리량 | — | **초당 972줄** (ain 724 + i2c 239) |
| 링크 유실 | — | **0.015 %** (60초 58,315줄 중 9줄) |
| 잡음(J3 raw 폭) | 1,396 | 3,579 (2.56배) = 유압 0.043 % → **0.11 %** |

🔴 **잃는 것은 송신이지 수집이 아니다.** 큐 `drops` 는 일곱 채널 전부 0 이고, 표본은 다
뜨고 다 타임스탬프가 찍힌다. 사용자 기준("늦게 보내도 되지만 버리면 안 된다")을
만족한다. 잃는 9줄은 **송신 링**이 순간 포화에 버린 것이다(구멍이 1~2줄씩 흩어져 있다 —
통째로 잘리면 한 구멍에 수십~수백 줄이 몰린다).

**비교**: 선행 프로젝트 Q2 는 같은 2 Mbps 에서 **~2 % 유실**이 미해결로 남아 있다
(CLAUDE.md §1.2). 여기는 **0.015 %** 로 130배 낫다. 물리계층이 다른지(F103 CDC 브리지
경유 vs 직결) 구조가 다른지는 아직 모르지만, **이 보드에서 2 Mbps 가 쓸 만하다**는
실측은 생겼다.

**여기까지 오면서 실기기가 잡아낸 결함 둘** — 둘 다 시험은 다 통과했는데 실물에서만 났고,
**10 ms 로 올려야만 드러나는 종류**였다(100 ms 에서는 여유가 커서 안 보인다):

1. **송신이 뒤쪽 채널을 굶겼다**(`688ce00`). 매 틱 `ch=0` 부터 시작해 한 채널 큐를 통째로
   비우니, 16줄 예산을 앞쪽이 다 먹고 J8·J9 는 30초 내내 레코드가 0 이었다(drops 3,040).
   주석은 "버리는 것이 아니라 미루는 것"이라 적혀 있었는데 **미루기가 영원하면 버리는
   것**이다. 라운드로빈 + 한 바퀴에 채널당 한 표본으로 고쳤다
2. **송신이 슈퍼루프를 붙잡았다**(`a85e6ae`~`aabd6c1`). `mk_uart_write` 가
   `HAL_UART_Transmit` 블로킹이라 한 바퀴가 32 ms 였다(495줄/초 ÷ 16줄/틱 = 31틱/초 로
   역산됨). 수집은 인터럽트로 떼어냈는데 송신만 남아 있었다. 링버퍼 + DMA2 Stream0 으로
   바꾸니 큐 peak 이 64(가득) → 4 로 떨어졌다

**남은 큰 구멍**:

- 🔴 **송신 링 통계가 `$STAT` 에 없다.** 지금 `drops`·`peak` 를 보려면 GDB 로
  `p 'mk_uart.c'::s_tx` 를 해야 한다. 그래서 `seq` 구멍이 났을 때 **보드가 버린 것인지
  보드를 떠난 뒤 없어진 것인지 못 가른다** — 두 경우의 처방이 정반대다(전자는 필드·주기·
  baud, 후자는 F103·USB·호스트). Q2 가 멈춘 자리가 정확히 이 구분이 없던 자리다
- GNSS 시간축이 아직 `device_clock` 이다(실내라 fix 가 없다)
- 🔴 **`tx.period_ms` 는 ain 과 i2c 가 공유한다.** 10 ms 로 두면 I2C 도 100 Hz 로 나가
  300줄/초가 더 붙는다. 1.5 Mbps 에서 링이 넘친 원인이 이것이었다(2 Mbps 에서 해소).
  종류별 송신 주기를 나눌지는 미결

---

## 2. 지금 무엇이 되는가

실기기(COM23)에서 **확인된 것**만 적는다. 코드가 있어도 실물로 안 본 것은 §3에 있다.

| 기능 | 상태 | 어떻게 확인했나 |
|---|---|---|
| GUI·CLI로 설정 읽기·쓰기 | ✅ | `markon_cli --port COM23 list` |
| 저장 → 리셋 후 복원 | ✅ | `tools/verify_persist_rails.py` — 24V 켜고 저장, 리셋해도 살아 있음 |
| 전원 레일 3종 제어 | ✅ | 순차 기동·즉시 차단·5V 포함. `$STAT`과 GPIOD ODR이 일치 |
| ADS1256 7채널 수집 | ✅ | DRDY EXTI → SPI4 DMA → 채널별 큐. 타임스탬프 100 ms 간격 |
| **텔레메트리 송신** | ✅ | `b15227e`. 100 ms 주기, 50건 수신 / 누락 0 / 깨짐 0 |
| **송신 구조 — 수집·송신 분리** | ✅ | 2026-08-19(사용자 설계). `ain`·`i2c`는 채널·포트마다 "마지막 값" 하나만 들고 있다가 `tx.period_ms`마다 그대로 내보낸다 — 새 수집이 없으면 같은 값·같은 `t`(획득 시각)가 반복된다. 큐(`mk_queue`)는 그대로 두고(진단용) 그 옆에 추가했다. `din`은 그대로 엣지 기반. 규격 §7.1 참고 |
| **PGA·데이터율이 칩에 닿음** | ✅ | `4cb7b0c`. J4 각 100건 — 30,000 SPS 폭 6,577 → 60 SPS 폭 1,449 |
| **아날로그 값이 안정** | ✅ | `f5af9ac`. J4 99건 전부 798,182~804,759 (평균 3.98 mA) |
| **디지털 입력 J20 (옵토)** | ✅ | 2026-08-19. 90회 변화를 **하나도 안 놓쳤다** — 0↔1 이 완벽히 번갈아 나왔고 같은 상태가 연달아 나온 곳이 없다(디바운스가 채터를 거르고 엣지도 안 놓쳤다는 뜻). 극성도 맞다(신호=1). J18·J19 는 같은 코드 경로이나 미배선 |
| **I2C — MLX90614(J12)·AM2320(J13)** | ✅ | 2026-08-19. 적외 24.15 °C / 접촉 24.1 °C·56.1 % — **원리가 다른 두 센서가 0.05 °C 안에서 일치**했다. 변환식이 맞다는 실증이다(÷10 과 `raw×0.02−273.15` 는 완전히 다른 계산). 같은 버스(I2C5)에 주소 다른 둘(0x5A·0x5C)이 함께 붙는 것도 확인 |
| **아날로그 J3 — 유압 0~250 bar** | ✅ | 2026-08-19. `pwr.24v` 를 켜야 루프에 전류가 흐른다(끄면 0 mA). 3.98 mA = 대기압 0 bar. 잡음 ±0.06 bar(만점의 0.05 %), 영점 치우침 −0.33 bar(0.12 %)는 GUI 영점 잡기로 눌린다 |
| **GNSS — 양방향 통신** | ✅ | 2026-08-19. 보드가 UM981 에 47자 명령을 보내고 `response: OK` 를 받았다. `$GNRMC`/`$GNGGA` 로 **UTC 날짜·시각 수신**(위성 2~3개). fix 는 실내라 아직 |
| **GNSS — PPS 입력 캡처** | ✅ | 2026-08-19. GDB 로 확인 — `s_pps_dev_us` 가 1,003,109 µs 간격으로 오른다(정확히 1 Hz). PC8 핀 토글·TIM8 CCR3 갱신·슈퍼루프 소비까지 전부 정상. 🔴 `pps_age_ms` 가 null 인 것은 **짝지을 유효 RMC 가 없어서**지 PPS 가 없어서가 아니다 — `pps_raw_count`·`pps_unpaired_reason` 이 그것을 말해준다 |
| **LCD (J25, ILI9488)** | ✅ | 2026-08-19. 백라이트·SPI2·DMA·주소창·18bpp·색순서·방향까지. 🔴 **MADCTL = 0x48 은 데이터시트가 아니라 실물이 정했다**(BGR·MX 둘 다). 한 색으로 채우는 것만으로는 방향 결함을 못 잡는다 — 글자가 나온 뒤에야 보였다 |
| **LCD 가 수집을 안 민다** | ✅ | 2026-08-19. 화면을 그리는 동안 `ain` 간격 중앙 100 ms / 최대 104 ms, 큐 `depth 0 / peak 2 / drops 0`. 전면 갱신이 460,800 B(≈184 ms)인데 **한 바퀴에 한 행**씩만 내보내 슈퍼루프를 안 붙잡는다 |
| **WS2812 송신 경로** | ✅ | `9ab5b36`. PA7=AF2, TIM3 ARR=79 PSC=0(800 kHz), DMA 완료 콜백 옴 |
| `$STAT`이 실제 핀을 말함 | ✅ | `rails{v24,v14v9,v5}=true` ↔ `GPIOD ODR = 0x700` |
| **시스템 클럭 = 25 MHz 크리스털** | ✅ | 2026-08-20. `RCC_CFGR=0x1b`(SWS=PLL1) · `RCC_CR=0x0303c025`(HSERDY·PLL1RDY) · `PLLCKSELR=0x02020052`(PLLSRC=HSE, DIVM1=5) · `PLL1DIVR=0x0707127f`(DIVN1=128, DIVP1=10) → 25÷5×128÷10 = **64.000000 MHz**. 네 레지스터가 계산한 기댓값과 정확히 일치. 전원을 껐다 켜도 유지된다. `$STAT.clock.src="hse_pll"` |
| **SPI4 커널 클럭 500 kHz** | ✅ | 2026-08-20. `D2CCIP1R=0x4000`(SPI45SEL=000=APB2) · `SPI4_CFG1=0x60070007`(MBR=6=÷128) → 64 MHz÷128 = **500 kHz**. 코드가 가정하던 값과 일치 — §5.8 의 t6 대기 계산이 안전하다는 뜻이다(전까지 `[미확인]`이었다) |
| **호스트 링크 baud 전환** | ✅ | 2026-08-20. `link.baud` 설정 항목(115200~2000000). 실기기에서 921600 → 1.5 M → 2 M 전환 확인. 🔴 **안전장치가 본체다** — 응답을 옛 속도로 다 보낸 뒤 전환하고, 10초 안에 `$BAUD,CONFIRM,<값>` 이 안 오면 **스스로 옛 속도로 돌아온다**. 확정 전에는 플래시에 저장되지 않는다(저장했다면 리셋마다 못 붙는 보드가 되고, 그건 굽기로만 풀린다) |
| **10 ms 수집 (7채널)** | ✅ | 2026-08-20 — §1 의 표. TIM7(1 kHz)이 수집 상태머신을 깨우고, SPI 완료 ISR 이 다음 채널로 곧바로 잇는다. `main()` 은 `mk_ads_tick()` 을 부르지 않으며 부르면 `host/tests/test_firmware_acquisition.py` 가 깨진다 |
| **송신이 슈퍼루프를 안 막음** | ✅ | 2026-08-20. `app/mk_txring` + DMA2 Stream0(USART3_TX). `mk_uart_write()` 가 즉시 돌아온다. 링 4,096 B, 제어 경로용 예약 512 B(텔레메트리가 링을 다 먹어 `$SACK` 이 못 나가면 되돌릴 수단이 사라진다). 링이 차면 **줄을 통째로 버린다** — 부분 삽입은 NDJSON 을 깨뜨린다 |
| 굽기 | ✅ | `tools/flash_verified.py` — 세 번 읽어 다수결. 왜 세 번인지는 §5.1. 🔴 **막히면 전원을 빼고 20초 기다린다**(CLAUDE.md §4 — 짧게 껐다 켜는 것으로는 안 풀렸다) |

**시뮬레이터에서만 확인된 것** (실기기 미검증 — §3):
설정 항목 93개(디지털 출력·LED·I2C 포함), 제어 모드 TEST/ACTIVE, 계기판 GUI 전체,
영점 잡기 패널, I2C 대시보드.

### 되는 것을 직접 해 보려면

```bash
# 실물 보드 (--port 필수. 시뮬레이터는 2026-08-20 에 삭제됐다 — CLAUDE.md §0)
python -m host.gui.app --port COM23

# 보드용 이미지 빌드 — 툴체인은 PATH 에 없고 CubeIDE 안에 있다
CUBE=/c/ST/STM32CubeIDE_2.1.1/STM32CubeIDE/plugins
PATH="$CUBE/com.st.stm32cube.ide.mcu.externaltools.gnu-tools-for-stm32.14.3.rel1.win32_1.0.100.202602081740/tools/bin:$CUBE/com.st.stm32cube.ide.mcu.externaltools.make.win32_2.2.100.202601091506/tools/bin:$PATH" \
  make -C firmware/stage1

# 굽기 — 반드시 이것으로. 이유는 §5
cd firmware/stage1 && python tools/flash_verified.py

# 시험 (보드 불필요)
python -m pytest -q                                   # 990 passed, 166 skipped
powershell -ExecutionPolicy Bypass -File firmware/stage1/tests/run_tests.ps1
                                                      # C 20묶음 + 대조 10종
```

보드용 빌드는 호스트 시험이 못 잡는 것을 잡는다 — 링크는 ARM 빌드에서만 깨지고
(`tests/check_sources.py` 의 사연), 경고도 다르다. 이번에 `-Wtype-limits` 하나가
거기서만 나왔다. MSVC 는 조용했다.

`-ExecutionPolicy Bypass` 가 없으면 이 컴퓨터에서 스크립트가 아예 안 뜬다
(`UnauthorizedAccess`). 시험이 깨진 것으로 오해하기 쉽다.

pytest 의 skipped 가 많은 것은 정상이다 — `test_firmware_safety.py` 가 펌웨어
소스를 파일마다 훑으면서, 그 핀을 안 건드리는 파일을 건너뛴 수다. 소스가 늘면
이 수도 함께 는다.

---

## 3. 안 되는 것 / 확인 안 된 것

**막힌 것은 없다.** 아래는 "아직 안 만들었다"거나 "확인할 방법이 없었다"다.

| | 상태 |
|---|---|
| **J18·J19 옵토** | 🟡 **[미검증]** J20 은 실물로 확인됐다(§2). 나머지 둘은 **같은 코드 경로**이고 배선만 안 됐다 |
| **BH1750 조도** | 🟡 **[미검증]** 드라이버는 섰지만 센서가 아직 안 물렸다. 같은 표를 쓰는 MLX90614·AM2320 이 실물로 확인됐으므로 경로 자체는 검증된 셈이다 |
| **LCD 화소 깨짐** | ✅ **원인은 배선이다**(사용자 확인 2026-08-19). 처음엔 "무작위"로 보였으나 **케이블을 만지면 깨진다**는 것이 드러났다 — 진동·미세한 움직임이 원인이었다. 펌웨어로 고칠 문제가 아니고 점퍼선을 짧게·단단히 물리는 것이 근본 해결이다.<br><br>가는 길에 제외된 후보들: **캐시 일관성**(이 펌웨어는 `SCB_EnableDCache` 를 안 부른다), **터치 칩 간섭**(PD14 를 능동 High 로 몰고 있고 값→방향 순서도 맞다), **전원계 노이즈**(24V 스위칭과 무관).<br><br>🔴 **깨진 뒤 저절로 안 돌아오던 이유**는 부분 갱신이 값이 바뀐 칸만 다시 그리기 때문이다 — 정지한 칸은 깨진 채로 남는다. `lcd.redraw_ms`(기본 60 s)가 주기적으로 전면을 다시 그려 이것을 덮는다. **배선을 고쳐도 이 그물은 남겨 둔다** |
| **LCD 되읽기 진단** | ❌ **이 모듈은 못 쓴다.** SDO 로 값을 안 돌려준다(3.5인치 모듈에 흔하다 — SDO 미결선이거나 단방향 레벨시프터). 초기화 직후 우리가 방금 써넣은 MADCTL 을 되물었더니 안 맞았고, 그래서 펌웨어가 **검사만 끄고** `$STAT.lcd.readback=false` 로 알린다. 멀쩡한 화면을 몇 초마다 재초기화하지 않기 위한 판단이다 |
| WS2812 발광 | **[미검증]** 스트립이 안 물려 있다. 송신 경로까지만 확인됐다(§2) |
| 제어 모드 실기기 | **[미검증]** 시뮬레이터와 C 단위시험에서만 확인 |
| 값 정확도 | **부분 확인.** J4 에 센서를 물려 3.98 mA 를 안정적으로 읽는다. **만재(20 mA)·직선성·다른 채널은 미검증** |
| **채널 오염 (정착 시간)** | **[미검증]** 채널을 바꾼 뒤 필터가 다시 차는 시간은 코드가 이미 지킨다(MUX→SYNC→WAKEUP→제 DRDY 대기, ADS1256.pdf p.21). 다만 **실물로 확인하지 않았다** — J3 에만 유압이 물려 있으니, **J3 에 계단 입력을 주고 빈 채널이 따라 움직이는지**(상관계수 \|r\|<0.2) 보면 갈린다. 2000 SPS 로 올린 지금이 가장 위험한 조건이다 |
| **송신 링 유실의 출처** | **[미확인]** 2 Mbps 에서 0.015 % 가 남는다. GDB 로 보면 링 `drops` 가 오르므로 **지금은 보드가 버리는 쪽**으로 보이지만, `$STAT` 에 없어서 실험할 때마다 확인할 수 없다 |
| 레일 실제 전압 | **[미검증]** 피드백 회로가 없어 보드도 모른다 |
| **GNSS 시간축** | 🟡 통신·PPS 캡처는 됐다(§2). 시간축이 아직 `device_clock` 인 것은 **실내라 fix 가 없어서**다 — RMC 가 `V`(무효)라 PPS 와 짝지을 수 없다. 🔴 **모듈 기본값 `CONFIG PPS ENABLE` 은 위치 fix 를 조건으로 건다**(`Unicore_N4_Commands.pdf` p.21 Table 4-5). `ENABLE2` 로 바꾸면 시각만 유효해도 PPS 를 내지만 수렴 전 **±100 ms** 라, 최고 등급은 여전히 RMC 유효일 때만 준다. 그리고 `SAVECONFIG` 를 안 하면 **전원을 껐다 켤 때 모듈 설정이 날아간다**(실증) |
| 실외 fix | **[미검증]** 실내에서 위성 2~3개까지 잡힌다. fix 는 4개 이상 필요 |
| 워커 한 바퀴 200 ms | 보드는 100 ms로 보내는데 화면의 `표본 간격`이 202 ms다. 원인 미확인 |

---

## 4. 코드가 어떻게 나뉘어 있나

### 펌웨어 — `firmware/stage1/`

```
app/     HAL 비의존. 보드 없이 호스트에서 컴파일해 시험한다 (16개 모듈)
  mk_framing   mk_json    mk_hostlink  mk_config   mk_cfgwire
  mk_cfgtable  mk_crc     mk_queue     mk_ads1256  mk_railctl
  mk_telem     mk_ws2812  mk_solctl    mk_i2c      mk_i2c_drivers
  mk_i2c_bh1750
bsp/     HAL이 나오는 유일한 층
  mk_uart  mk_flash  mk_time  mk_ads_io  mk_rails  mk_ws2812_io  mk_sol
  mk_i2c_io  mk_dma_mem.h  stm32h7xx_it
main.c   슈퍼루프
```

**이 경계가 이 프로젝트의 전제다.** `app/`이 HAL을 import하지 않기 때문에
호스트에서 그대로 컴파일되고, Python 시뮬레이터와 바이트로 대조할 수 있다.
`test_firmware_safety.py`가 그 경계를 강제한다.

🔴 **방향은 한쪽이다.** `bsp/`가 `app/`을 include하는 것은 되고 반대는 안 된다.
Flash staging 버퍼가 그 예다 — 필요한 크기를 `app/mk_config.h`의
`MK_CFG_BLOB_MAX`가 선언하고 `bsp/mk_flash.c`가 그것을 쓴다.

🔴 **여기 없는 것이 곧 §3의 구멍이다.** `mk_sol`·`mk_i2c` 계열은 이제 자리를
채웠지만 **둘 다 보드로 확인한 적이 없다**(§3). 화면에 탭이 뜬다고 값이
하드웨어에 닿았다는 뜻은 아니다(§8) — 카탈로그만 있고 코드가 없던 시절과
증상이 똑같이 보인다.

### 호스트 — `host/`

```
core/     framing · records · config_schema · scaling · limits · errors
service/  board_service (연결·명령응답·유실집계)
gui/
  screen.py       🔴 화면이 무엇을 보여야 하는가. Qt를 import하지 않는다
  settings_form.py  표 접기·전송 모양·채널 범위. 역시 Qt 없음
  tare.py         영점 잡기의 판정과 규칙. 역시 Qt 없음
  field_budget.py   필드 선택이 전선에 얼마를 쓰는가
  theme.py        색·간격 토큰만. 스타일시트는 qt/style.py 로 갈라져 있다
  qt/view.py      뷰 계약 — render(state) 하나
  qt/*.py         어떻게 그리는가
  app.py          배치 + 배선
```

**Qt 를 모르는 층은 이제 시험이 강제한다** — `test_layer_boundaries.py` 가
디렉터리를 훑어 AST 로 본다(`7944ea8`). 새 모듈을 넣으면 저절로 걸리므로
경계를 어겼는지 손으로 볼 필요가 없다.

**UI를 바꿀 때 어디를 건드리나:**

| 하고 싶은 것 | 고칠 곳 |
|---|---|
| 배치만 바꾼다 | `app.py`의 레이아웃 + `_views` 목록. **`_on_step`은 안 건드린다** |
| 뷰를 새로 만든다 | `render(state)` 하나만 구현하면 데이터가 알아서 온다 |
| 보여줄 것 추가 | `ScreenState`에 필드 + `build_screen`에서 채움 |
| **설정 항목 추가** | **보드(시뮬레이터+펌웨어)에만 넣는다.** 화면은 카탈로그를 보고 저절로 그린다 |

마지막 줄이 실제로 증명됐다 — `sol`·`led`·`i2c` 41항목을 넣었더니
호스트 코드는 그룹 이름표와 표 접기 규칙만 고치고 탭 셋이 생겼다.

---

## 5. 🔴 밟으면 시간을 크게 버리는 함정

전부 **실제로 밟아 본 것**이다. 순서는 아픈 순.

### 5.1 굽기가 조용히 실패한다 — `compare-sections`를 믿지 마라

모든 섹션이 `matched`로 나왔는데 플래시를 되읽어 보니 **마지막 1,439바이트가
통째로 `0xff`** 였다. 하필 거기가 `.init_array`라 부팅 즉시 HardFault.
증상은 **"보드가 조용하다"** 뿐이다.

→ **항상 `tools/flash_verified.py`로 굽는다.** `flash.gdb`를 직접 쓰지 않는다.

**되읽기도 흔들린다** [실증 2026-08-17]. 같은 gdb 세션에서 같은 플래시를 세 번
읽었더니 세 번이 다 달랐다(16·12·4바이트, `00` ↔ `ff` 로 통째로). 읽기 패킷을
256+fixed 로 고정해도 줄기만 하고, SWD 782 kHz 로 낮춰도 그대로다.

→ **한 번 읽고 "다르다"고 판정하면 안 된다.** 멀쩡한 판을 스무 번 넘게 다시
구웠다. `flash_verified.py` 가 세 번 읽어 다수결을 내고 흔들린 자리 수를 함께
알린다 — **그 수가 크면 다시 굽지 말고 링크를 본다.** 성공한 회차에도 8자리가
흔들렸다.

**`restore` 로는 플래시를 못 굽는다.** gdb 의 `restore` 는 평범한 메모리
쓰기라 **성공한 얼굴로 아무 일도 하지 않는다** — 6번을 "구우고도" 예전 내용이
그대로였다. raw 백업은 `tools/restore_verified.py` 로 되돌린다(ELF 로 감싸
`load` 를 태운다).

### 5.2 DTCM에 DMA 버퍼를 두면 전송이 조용히 안 된다

`static uint8_t rx[3];`는 DTCM에 잡히는데 DMA1·DMA2는 거기 닿지 못한다
(RM0468 p.140, p.106). 컴파일·링크 다 통과하고 경고도 없다.
→ `bsp/mk_dma_mem.h`의 `MK_DMA_BUF`. 잊어도 `tools/check_dma_placement.py`가 막는다.

### 5.3 D2 SRAM 클럭이 리셋 직후 꺼져 있다

→ `__HAL_RCC_D2SRAM1_CLK_ENABLE()` / `D2SRAM2`.

### 5.4 STM32H7의 SPI는 DMA 완료가 아니라 자기 EOT로 알린다

DMA는 다 옮겨 놓고도 SPI4_IRQn을 안 걸면 완료 콜백이 안 온다.

### 5.5 DTR/RTS를 세운 채 포트를 열면 보드가 멈춘다

`.NET SerialPort`는 열 때 둘 다 세운다 — **쓰지 않는다.**
pyserial을 쓰되 `s.dtr = False; s.rts = False`를 **열기 전에** 설정한다.
멈췄으면 굽지 말고 GDB로 리셋한다 (`tools/reset.gdb`).

### 5.6 ADS1256의 아날로그 전원은 5V 레일이다

PD10이 Low면 **SPI 레지스터는 정상 응답하는데 변환이 안 되고 DRDY가
영영 안 떨어진다.** 채널마다 타임아웃만 쌓이면 배선이 아니라 레일부터 본다.

### 5.7 Qt — 위젯이 창 바탕을 칠한다

- 전역 `QWidget { background }`가 **모든** 위젯에 걸린다. `QLabel`·`QCheckBox`가
  흰 카드 위에서 각자 회색 사각형을 찍었다 — 설정 화면 오른쪽 절반이
  회색 띠밭이던 원인. `theme.py`에서 한 번에 막았다
- 순수 `QWidget`은 스타일시트 배경을 **스스로 그리지 않는다.** `QFrame` +
  `WA_StyledBackground`
- 인라인 `setStyleSheet`는 부모 규칙을 **통째로 덮는다**
- `QFrame.HLine`은 두꺼운 띠로 나온다. `qt/parts.hairline()`을 쓴다
- `QLabel`은 마크다운을 렌더링하지 않는다
- **공백 없는 긴 문자열은 `setWordWrap`으로 안 접힌다.** JSON 미리보기가
  카드 밖으로 삐져나갔다 — 쉼표 뒤에 폭 없는 공백(`\u200b`)을 넣는다
- `cv.height = …`처럼 **속성에 대입하면 원본이 덮인다.** 그 값을 매 프레임
  원본으로 다시 읽으면 캔버스가 배씩 자란다 (Artifact에서 겪음)

### 5.8 ADS1256은 명령과 데이터 사이에 쉬어야 한다

RDATA 명령과 데이터 3바이트를 한 번의 4바이트 전송으로 붙여 보냈더니 값이
**±전 범위로 튀었다.** 4~20 mA 루프는 120Ω 션트에서 0.48~2.4V 라 음수가
나올 수 없으니 잡음이 아니었다.

`ADS1256.pdf p.6 "TIMING CHARACTERISTICS FOR FIGURE 1"`: `t6` = 명령의 마지막
SCLK 에서 DOUT 첫 SCLK 까지 **MIN 50 tCLKIN** (7.68 MHz 라 6.51 us). 붙여
보내면 간격이 한 주기뿐이라 3배 넘게 모자란다. 칩이 DOUT 을 아직 안 실은 채로
클럭을 받고, 칩의 7.68 MHz 는 우리와 비동기라 **매번 다르게 실린다** — 그래서
잡음처럼 보인다.

`t11`(SYNC 뒤 24 tCLKIN = 3.13 us)도 같은 표에 있다. 안 지키면 SYNC 가 먹지
않아 변환이 **이전 채널 것으로 남는다.**

쉬는 시간은 `MkAdsIo.delay_us` 로 뺐다 — `app/` 은 HAL 을 모르는 층이라 직접
기다릴 수단이 없다.

### 5.9 설정 항목이 320바이트를 넘으면 조용히 사라진다

카탈로그 한 줄이 `mk_cfgwire` 의 320바이트 버퍼를 넘으면 **그 항목이 통째로
빠진다.** 안내문을 길게 썼다가 6개가 사라졌다. `cfg_end` 의 count 대조가
잡았다 — 그게 없었으면 GUI 에 항목이 안 보이는 이유를 한참 찾았을 것이다.

### 5.10 PowerShell로 소스를 치환하지 마라

`.Replace()`가 CRLF 때문에 **여섯 번** 조용히 실패했고, 나중에는
`Set-Content -Encoding utf8`이 한글 파일을 통째로 깨뜨렸다.
→ **`Edit` 도구를 쓴다.** 여러 파일을 한 번에 고쳐야 하면 파이썬 스크립트로.

---

## 6. 하드웨어 사실 (넷리스트·데이터시트로 확인한 것)

`docs/datasheet/STM32_v2.0_DATASHEET.md`는 2차 자료다. 하드웨어 판단은
`D:\STM32_PCB_Board\output\datasheet_src\STM32_v2.0_current.net`을 본다.

```
ADS1256 (U9)   PE11 CS · PE12 SCLK · PE13 DOUT · PE14 DIN · PE15 DRDY
               PE9 SYNC/PDWN · PE10 RESET
               전원: AVDD·VREFP = V5 레일 / DVDD = V3V3 상시

전원 레일      PD8 = 24V · PD9 = 14.9V · PD10 = 5V · PD11 = 상태 LED

디지털 입력    J18 = PA4 · J19 = PA5 · J20 = PA6   (데이터시트 §5.7)
               🔴 MCU GPIO 직결 — 커넥터 pin1 이 MCU 핀에 바로 온다. 사이에
                  부품이 없다. 옵토커플러는 **커넥터 반대편(외부)**에 있고,
                  보드는 그 신호를 **읽는다**(사용자 확정 2026-08-18, 넷리스트
                  확인). §6 은 예전에 "출력으로 확정"이라 적었으나 틀렸다 —
                  출력으로 잡으면 반대편 포토트랜지스터와 맞선다
               🔴 PA4·PA5는 3.3V 전용(절대최대 4.0V). PA6만 5V 허용 — 옵토가
                  앞에 있어 바깥 전압이 핀에 직접 닿지 않는다
               방향은 **입력**으로 확정. 보드는 3채널, 하우징은 2채널
               raw LOW = 옵토 도통(신호 있음) = state 1(켜짐), 극성 반전은
                  `mk_solctl.c` 한 곳에서만 한다(§7.1)

WS2812 (J21~J24)  PA7 → R84(390Ω) → WS_D1 → J21 pin1
               데이지체인 J21→J22→J23→J24, J24 Dout 미연결
               전원 pin2 = V5 → **PD10을 올려야 켜진다**
               J21부터 순서대로 안 채우면 뒤쪽이 죽는다
               PA7 AF2 = TIM3_CH2 (DS13313 Rev 1 p.72 Table 8)
               DMA1 Stream2 를 쓴다 — Stream0·1 은 SPI4(ADS1256)가 이미 쓴다
               🔴 색 순서는 데이터시트로 못 정한다. 스트립에 올라간 칩에
                  달린 값이라 `led.grb` 설정으로 뺐다. **이 보드에 물린
                  스트립은 RGB** 였다(기본값). 파랑은 두 순서 모두 셋째라
                  판별에 쓸 수 없다

I2C            I2C3 = J10/J11 · I2C5 = J12/J13 · I2C1 = J14/J15
               🔴 짝 커넥터는 **같은 버스**다 — 같은 주소면 충돌
               풀업 4.7 kΩ ×2 온보드(R25~R30). JP1~JP3로 3.3V/5V 선택
```

**ADS1256 정착시간** — `ADS1256.pdf, p.20, Table 13`. 표 16행 전부가
`t18 = 1/DRATE + 0.18 ms`에 맞는다. 60 SPS → 16.84 ms, 7채널 한 바퀴 약 118 ms.
**DMA로 못 줄인다** — ΔΣ 필터의 물리다. 주기를 줄이려면 DRATE를 올린다.

**ADS1256 만재는 VREF 가 아니라 2·VREF 다** — `p.11 "full-scale input range is
±2VREF (for PGA = 1)"`. VREF 로 두면 **모든 값이 정확히 절반**으로 나온다
(4 mA 신호가 1.99 mA). 배수가 딱 2 라 눈치채기 어렵다.

**ADS1256 레지스터 부호는 표로 옮긴다** — `p.32 "Make sure to select a valid
setting as the invalid settings may produce unpredictable results"`. 계산으로
만들 수 없다. 모르는 값이 오면 1배·60 SPS 로 간다 — 모르는 값에 64배를 주면
입력이 즉시 포화해 "센서가 이상하다"로 보인다.

**ADCON 상위 비트는 0 으로 둔다** — 리셋값 `0x20` 은 CLKOUT 으로 7.68 MHz 를
내보낸다. `p.31 "When not using CLKOUT, it is recommended that it be turned
off"`. 안 쓰는 클럭은 잡음만 는다.

---

## 7. 다음에 할 일

### 7.1 🟡 sol — 코드는 섰고 **보드에서 확인만 남았다**

🔴 **디지털 입력이다** (사용자 확정 2026-08-18, 넷리스트 확인 — §6). 옵토커플러가
커넥터 반대편(외부)에 있고 보드는 그 신호를 읽는다. `sol.j18` 같은 "켜라/꺼라"
설정 항목은 없다 — 입력에는 성립하지 않는 개념이라 카탈로그에서 뺐다. 남은
설정은 `sol.debounce_ms`(필터 값) 하나뿐이다.

🔴 **상태의 근거는 레벨이지 엣지가 아니다** (사용자 확정 2026-08-18 — "엣지만
보는게 아니라 그 상태를 읽어야 하는거야 엣지는 딱 감지하려고 하는거고").

- `bsp/mk_sol.c` 의 `mk_sol_read()` 가 `MkSolRead` 콜백으로 `mk_solctl_init()`
  에 등록된다. `app/mk_solctl.c` 의 `mk_solctl_tick()` 이 **매 바퀴** 이
  콜백으로 세 핀을 직접 읽어 디바운스와 확정 상태를 그 값에 건다.
- EXTI 엣지(`mk_solctl_on_edge()`)는 남아 있지만 역할이 바뀌었다 — 상태를
  바꾸는 근거가 아니라 **"언제 바뀌었나"의 정밀 시각**만 댄다. 큐에 쌓인
  엣지가 폴링된 레벨과 일치하면(정상 경로) 그 정밀 시각을 쓰고, 안 맞으면
  (엣지를 놓쳤다는 뜻 — 큐 경합·인터럽트 지연·잡음) 그 tick 의 시각으로
  새 후보를 열어 회복한다. 정밀도는 잃어도 상태는 다음 안정 구간에서
  반드시 맞아진다(자가회복).
- `mk_solctl_prime()` 은 남겼다 — 부팅 직후 첫 `mk_solctl_tick()` 이 돌기
  전까지의 공백(디바운스를 기다리는 동안 `$STAT` 이 "아직 모름"을 답하는
  것)을 없애려는 것이다. 매 tick 레벨을 읽는 지금도 이 이유 하나는 여전히
  유효하다.
- `tests/test_sol.c` 가 "엣지를 하나도 못 받아도 폴링만으로 회복한다"는
  자가회복과, "폴링 콜백이 없으면(예전 동작) 회복하지 못한다"는 되돌림을
  둘 다 시험으로 못박아 뒀다.

`main.c` 가 매 바퀴 `mk_solctl_tick()` 을 부르고, `mk_telem_tick()` 이 확정된
상태 변화를 `din` 레코드로 낸다(규격 §7.6). `$STAT` 은 그때그때의 확정 상태를
그대로 싣는다(규격 §7.4) — 연결 직후 호스트가 이걸 한 번 읽으면 엣지를 하나도
못 봤어도 지금 상태를 안다.

**보드 미연결로 못 한 것** — 다음에 보드가 오면 이것부터 한다:

```bash
cd firmware/stage1 && python tools/flash_verified.py
python -m host.gui.app --port COM23      # 대시보드의 "디지털 입력" 카드를 본다
```

- 🔴 **입력이므로 GUI 에서 켤 수 없다.** J18~J20 에 실제 신호(외부 옵토가
  도통하는 상황)를 걸어야 상태가 바뀐다 — 설정 탭에 스위치가 없는 것이
  정상이다(예전 계획과 다르다, §6)
- PA4(J18)·PA5(J19)·PA6(J20) 이 신호 없을 때 풀업으로 HIGH 인지, 신호를 걸면
  LOW 로 끌리는지 테스터로 본다
- 대시보드의 "디지털 입력" 카드가 그 변화를 즉시 따라가는지, `$STAT` 의 `din`
  이 재연결 직후에도 지금 상태를 맞게 답하는지 본다
- 🔴 PA7(WS2812)이 함께 죽지 않았는지 LED 체인도 같이 본다 — 같은 포트다.
  `mk_sol_init` 이 PA7 을 건드리지 않게 해 뒀지만 실물로 확인한 적은 없다
- 신호선을 잠깐 끊었다 붙여 채터링을 흉내 내 보고, 화면이 튀지 않고
  디바운스 뒤에만 확정되는지 본다 — 자가회복 로직이 실기기 잡음에서도
  버티는지의 첫 확인이다
- 제어 모드 TEST/ACTIVE 가 실기기에서 처음 시험되는 자리이기도 하다(§3)

**시험이 지키는 것** (`tests/test_sol.c`, `test_firmware_safety.py`)

- 엣지를 하나도 못 받아도(ISR 유실을 흉내 냄) 폴링만으로 확정 상태가
  레벨을 따라간다 — 자가회복의 핵심
- 폴링 콜백을 등록하지 않으면(예전 동작) 놓친 엣지에서 회복하지 못한다는
  되돌림 검사 — 자가회복 로직이 조용히 사라지면 이 시험이 잡는다
- 엣지가 있으면 레코드의 `t` 가 엣지 시각이고, 없으면(폴링만으로 확정됐으면)
  폴링 시각이다
- 레벨이 흔들리는 동안에는(엣지든 폴링이든) 확정하지 않는다(디바운스)
- 설정표를 **진짜 카탈로그**(`mk_cfgtable_init`)로 만들어 시험한다. 가짜 키로
  시험하면 카탈로그에서 이름이 바뀌어도 통과한다
- `sol.j18`~`sol.j20` 같은 켜기/끄기 설정이 카탈로그에 없다는 것을 못박는다
  — 남아 있으면 화면에 동작하지 않는 스위치가 뜬다
- GPIOA 에서 이 세 핀을 만지는 파일은 `mk_sol.c` 하나여야 한다 (레일과 같은 방식.
  GPIOA 자체는 WS2812 도 쓰므로 **핀 번호까지** 봐야 가려진다)

### 7.2 🟡 I2C — 코드는 섰고 **보드에서 확인만 남았다**

`app/mk_i2c`(포트별 상태기계 OFF→START→WARMUP→READY, 한 바퀴에 포트 하나,
실효 주기 = max(설정 주기, 변환 시간), 드롭 카운터) + `app/mk_i2c_drivers`
(종류→드라이버 표, 플러그인) + `app/mk_i2c_bh1750`(조도, 첫 드라이버) +
`bsp/mk_i2c_io`(I2C1·I2C3·I2C5 를 연다, 여섯 핀 전부 AF4, TIMINGR=0x60702729,
오픈드레인·내부 풀업 없음) 로 섰다. `mk_telem` 이 `i2c` 레코드를 낸다
(규격 §7.5, `ain` 과 seq·마스크를 나눠 쓰고 값이 없으면 null). 지원하지
않는 종류는 규격 §7.5 의 `status=3` 으로 호스트가 "지원 안 함"이라 보여준다.
C 시험 14묶음(대조 7종 포함)이 다 통과했고 ARM 빌드 링크도 성공했다 —
**그런데 보드가 없어 핀 파형·실제 센서 값·오류 상태는 아무도 못 봤다.**
지금 대시보드에 뜨는 값은 여전히 시뮬레이터가 만든 데모다.

**보드가 오면 이것부터 한다** (Task 8 브리프 Step 1~4 그대로):

```bash
cd firmware/stage1 && python tools/flash_verified.py
python -m host.gui.app --port COM23
```

- J10 에 BH1750 을 꽂는다. 🔴 **JP1 점퍼가 3.3V 인지 먼저 확인** — 5V 로
  두면 풀업도 5V 가 되어 3.3V 전용 핀 쪽을 넘길 수 있다
- 설정 탭에서 `i2c10.종류` = 조도, `i2c10.사용` = 켬, `i2c10.주소` = 0x23(35).
  대시보드에 lx 카드가 뜨는지 본다
- 센서를 뽑았다 꽂아 본다 — 뽑으면 `status=1`(값 없음)로, 꽂으면 값이
  돌아오는지 확인한다
- I2C 가 도는 동안 ADS1256 값이 계속 오고 `$STAT` 의 `drops` 가 안 오르는지
  본다(`markon_cli --port COM23 monitor --seconds 20`) — 아래 60ms 블로킹이
  실사용에서 표본을 잃지 않는다는 것의 실기기 확인

**🔴 최악 블로킹은 60ms 다.** HAL 이 전송 전 버스 BUSY 를 고정 25ms
(`I2C_TIMEOUT_BUSY`, `stm32h7xx_hal_i2c.c:344`) 로 기다리고 우리 타임아웃이
5ms 더 있어 xfer **한 번**의 최악이 30ms — 그런데 BH1750 의 `start`
(Power On·모드 선택)는 STOP 을 사이에 두고 xfer 를 **두 번** 나눠 부르고,
`mk_i2c_tick()` 은 이 둘을 같은 스텝(한 바퀴) 안에서 잇달아 부른다(검토
지적 I5 — 예전에는 30ms 로 적었으나 셈이 한 번짜리 xfer 만 본 것이었다).
한 바퀴에 포트 하나뿐이라 60ms 도 한 바퀴에 최대 한 번만 선다 — 그래도
버스 잠김 복구(SCL 토글)는 아직 안 넣었다. 계획서 §14 대로 **실제로 겪은
뒤에** 넣는다.

🔴 **"ADS1256 은 DRDY 인터럽트+DMA 라 표본을 안 잃는다" 는 2026-08-19
이전까지 사실이 아니었다.** 변환 자체는 인터럽트로 돌았지만 **다음 채널을
시작하는 것**은 슈퍼루프의 `mk_ads_tick()` 뿐이었다. 즉 이 60ms 동안 새
변환이 시작되지 않았고, `finish()` 가 따라잡기를 포기하므로 그 표본들은
큐의 `drops` 에도 안 잡힌 채 사라졌다(100ms 주기라 드물게 한 개씩).
지금은 진행이 전부 인터럽트에 있다 — TIM7(1 kHz)이 쉬던 상태머신을 깨우고,
SPI 완료 인터럽트 안의 `finish()` 가 밀린 다음 채널로 곧바로 이어 간다.
`main()` 은 `mk_ads_tick()` 을 부르지 않으며, 부르면
`host/tests/test_firmware_acquisition.py` 가 깨진다.

🔴 **PA8·PC9 는 AF6 에도 I2C5 가 있다.** AF 를 잘못 고르면 J10/J11 이
I2C3 이 아니라 I2C5 에 붙어 **아무 파형도 안 나간다** — 증상이 배선
불량과 똑같아서 헷갈리기 쉽다. 지금 코드는 데이터시트 Table 8(DS13313
Rev 1)로 AF4 를 확정해 썼다(§6).

**남은 것**:
- 온습도·적외 온도·방수 온도 드라이버 — 칩 모델이 정해지면 파일 하나씩
  (`mk_i2c_drivers` 표에 등록만 하면 되는 구조)
- 2차 — "직접 설정(raw)" 종류 (설계 문서 §13). 드라이버 하나로 들어가므로
  구조는 안 바뀐다
- `$STAT` 에 I2C 포트 상태 싣기
- 버스 잠김 복구(SCL 토글) — 위에서 미룬 항목, 실제로 겪은 뒤에

이번에 데이터시트로 참고 구현(LaneControlSystem Q2)을 두 군데 뒤집었다:
BH1750 은 **전원을 먼저 켜야** 한다(전원 인가 직후 Power Down 이 초기
상태라 모드 명령만 보내면 씹힐 수 있다) — Power On(0x01) → 모드(0x10),
각각 STOP 으로 끝난다. 변환 대기는 **120(typ) 이 아니라 180ms(max)** —
120 으로 읽으면 값이 나오긴 나오므로(변환이 덜 끝난 값) 눈으로는 못
가린다. 참고 구현만 믿었으면 둘 다 놓쳤다.

### 7.3 대시보드 `출력` 구획

지금 sol·led는 설정 탭에서만 만진다. 전원 레일처럼 대시보드에 두되
**제어 모드에 따라 다르게** 굴어야 한다 — TEST면 누르고 있기(손 떼면 원위치),
ACTIVE면 유지되는 명령. 이제 핀이 움직이므로(§7.1) 막는 것이 없다.

### 7.4 남겨 둔 작은 것들

- **GUI 설치 exe** (사용자 요청 2026-08-20, **마지막에** — 화면이 아직 매일 바뀐다). PyInstaller, 진입점 `host.gui.app` 하나, 보드는 표준 CDC 라 드라이버 동봉 불필요. exe 버전과 펌웨어 버전을 화면에 함께 띄울 것

- `check_dma_placement.py`가 이름 규칙 기반이라 불완전하다 (Codex 지적, 수용)
- `BoardService.records`가 무한히 자란다
- `LoopReading.broken=True`가 "데이터 없음"과 "물리적 단선"을 뭉갠다
- 워커 한 바퀴가 200 ms 걸린다 (§3)
- SPI4 커널 클럭 실측 → 분주비 확정 (§3). 지금은 가정 위에 있다
- 만재(20 mA)·직선성 검증. 지금은 4 mA 근처 한 점만 봤다
- `docs/status/PROJECT_STATUS.md`(Codex)가 **2026-08-14 기준으로 낡았다.**
  거기 적힌 "호스트 회귀 실패"는 이미 해소됐다 — 지금은 539 passed.
  🔴 그 파일은 **내가 고치지 않는다**(CLAUDE.md §6). 다음 점검 때 갱신된다

---

## 8. 작업 방식 — 값을 한 것

### 되돌림 검사 (revert-check)

**고칠 때마다, 가드를 빼면 시험이 실제로 깨지는지 확인한다.** 이것으로 찾은 것:

- 라운드로빈 시험이 **헛돌고 있었다** — 굶주림을 막는 것은 정렬이 아니었다
- DMA 검사 도구가 **아무것도 못 잡고 있었다** (`--gc-sections`가 프로브를 걷어감)
- `$STAT` 버퍼가 7채널을 못 담았다
- `MATRIX_MIN_ROWS` 가드를 빼니 2행짜리 표가 생겼다 — 시험이 문다

### 🔴 카탈로그에 있다고 되는 것이 아니다

이 설계의 장점이 곧 함정이다 — **설정 항목만 넣으면 GUI 탭이 저절로 생기므로,
값이 하드웨어에 닿지 않아도 화면은 완성돼 보인다.** 같은 빠짐을 네 번 밟았다:

| 무엇 | 어디까지 있었나 | 무엇이 없었나 |
|---|---|---|
| `ain*.enabled` | 카탈로그·저장 | 수집기에 닿는 선 |
| `adc.pga` · `adc.drate` | 카탈로그·저장 | 칩에 쓰는 WREG (MUX 말고는 아무것도 안 썼다) |
| `led.*` | 카탈로그·저장 | 핀으로 내보내는 코드 한 줄도 없었다 |
| `sol.*` | 카탈로그·저장 | 핀 레벨을 읽어 들이는 코드 — 2026-08-18 에 채웠다(같은 날 안에 출력→입력으로 방향이 바로잡혔다, §7.1) |

특히 `adc.drate` 는 **값이 정상으로 나오기 때문에** 아무도 눈치채지 못했다.
화면은 60 SPS 라 하는데 칩은 리셋 기본값 30,000 SPS 로 돌았다.

→ 설정 항목을 더할 때 **"이 값을 읽는 쪽이 어디인가"를 함께 적는다.**

### 구현과 시험이 같은 오해를 나눠 가지면 영원히 통과한다

전류 환산이 정확히 절반으로 틀렸는데(§6, 2·VREF) 시험이 못 잡았다. 시험이
잘못된 상수 `2.5` 를 **손으로 다시 적어** 기대값을 계산하고 있었기 때문이다.
시뮬레이터도 같은 식이라 대조로도 안 걸렸다.

→ 시험은 **구현 상수를 빌려 쓰고**, 그 상수 자체를 따로 못 박는다.

### 대조 (crosscheck)

C 펌웨어와 Python 시뮬레이터가 같은 입력에 같게 답하는지 6가지로 대조한다.
**`crosscheck_cfgtable.py`가 가장 값지다** — 두 카탈로그가 어긋나면 GUI는
시뮬레이터에서 멀쩡하다가 보드에서만 틀어진다.

실제로 잡았다: `i2c*.addr`의 하한을 펌웨어에서 0으로 고치고 시뮬레이터를
안 고쳤는데, 대조가 `minimum: 펌웨어=0 시뮬레이터=8`로 짚어 줬다.

**손으로만 돌리면 썩는다.** 지금은 `run_tests.ps1`에 매달려 있다.

### 시험이 개수를 하드코딩하지 않게

`assert len(form.keys()) == 45`가 세 군데 있었다. 하드코딩을 금지하는
시험이 스스로 목록 길이를 하드코딩한 셈이라, 보드에 항목이 늘 때마다 깨졌다.
지금은 카탈로그가 말하는 것과 대조한다.

### 근거 (CLAUDE.md §5)

부품 사양·회로·핀맵은 **추정하지 않는다.** 지금까지 확정한 것: ADS1256
정착시간·t6·t11·만재 2·VREF·레지스터 부호·ADCON CLKOUT, DTCM 접근 제약,
ADS1256 전원 경로, WS2812 배선과 PA7 AF2=TIM3_CH2, SPI4 배선,
디지털 출력의 구동 한계와 전압 내성, I2C 버스 짝짓기, I2C1·I2C3·I2C5
여섯 핀 전부 AF4(DS13313 Table 8)와 TIMINGR=0x60702729(ST 유틸리티로
64 MHz/100 kHz 산출), BH1750 의 Power On 선행·변환 대기 180ms(max).

**데이터시트로 정할 수 없는 것도 있다.** WS2812 색 순서가 그랬다 — 스트립에
올라간 칩에 달린 값이라 문서로 단정하면 틀린다. 코드에 박지 말고 설정으로
빼고, 기본값에 **어느 보드에서 확인한 값인지**를 적는다.

---

## 9. 참고

- `CLAUDE.md` — 규칙. 설계 원칙 4가지, 하드웨어 제약, git 규칙
- `protocol/specification.md` — 호스트·펌웨어 공통 계약 (schema_ver 3)
  - §6.4 제어 모드 TEST/ACTIVE · §7.2.1 물리량 환산식 — **둘 다 펌웨어가 지켜야 한다**
  - §7.3 `choice_labels` — 열거형 이름표. 길이가 다르면 호스트가 **통째로 버리고
    숫자를 보여 준다.** 짝이 어긋난 이름표는 화면이 멀쩡해 보이면서 엉뚱한 것을
    고르게 만든다
  - §7.5 I2C 레코드 · §7.5.1 `quantity` 어휘 — 펌웨어가 지킨다(시험·대조로 확인),
    **보드로는 아직 확인 못 했다**(§7.2)
- `docs/status/PROJECT_STATUS.md` — Codex 외부 감사 (내가 고치지 않는다)
- `firmware/stage1/tools/README.md` — 굽기·복구 절차
- `D:\STM32_PCB_Board` — KiCad 원본·넷리스트·부품 데이터시트 (읽기 전용)
- `LaneControlSystem` Q1/Q2 — 선행 펌웨어. **둘 다 H7A3용이라 바이너리·
  CubeMX 재사용 금지.** HAL 비의존 모듈의 로직만 본다
