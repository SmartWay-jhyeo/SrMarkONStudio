# HANDOFF 0831 — I2C 송신 구조 변경 방향 (사용자 결정, 2026-08-30)

> ✅ **[2026-08-31] 전부 구현 완료** — feat/cloud-unify 브랜치, 계획 1
> (펌웨어)·계획 2(호스트) 도합 20커밋. 계획 문서:
> `docs/superpowers/plans/2026-08-31-usb-cloud-unification-fw.md` / `-host.md`.
> **남은 것은 굽기뿐** — 사용자 통지 후 진행(HANDOFF.md 의 절차).
> 아래는 결정 당시의 기록 그대로다.

## 배경 — 현재 구조 (코드로 확인, 2026-08-30)

I2C 클라우드 경로에는 **송신 주기가 없다. 새 표본 1개 = 레코드 1줄**이다.

- `mk_cloud.c` `tick_i2c()`(mk_cloud.c:344)는 최신값 캐시(`mk_i2c_last`)를
  읽지만 `i2c_sent_t[p][k] == o.t_ms`면 건너뛴다 — 캐시는 **중복 송신
  방지용**이고, 타임스탬프가 새것일 때만 나간다. 수집 주기 = 송신 주기.
- 이는 2026-08-21 설계 그대로다(클라우드 스키마 설계 §4.7 "송신 주기 설정은
  두지 않는다 — 보드는 수집되는 대로 보낸다").
- 수집 주기는 실효 주기 = `max(i2cN.period_ms, warmup_ms)`(mk_i2c.c:283).
  warmup 은 드라이버 상수: **AM2320 온습도 2000ms** / MLX90614 250ms /
  BH1750 180ms. 온습도가 2초에 한 번인 것은 설정이 아니라 **센서 하한**이다.

## 결정 — 수집과 송신을 분리한다

새 값이 2초에 한 번인 것은 상관없다. **수집되는 대로 캐시에 덮어쓰고,
송신은 별도 주기로 캐시의 최신값을 읽어 보낸다.** 새 값 여부는 따지지 않는다.

- 예: 송신 주기 100ms 면 젯슨에는 100ms 마다 온습도 레코드가 도착하고,
  새 값이 올 때까지 같은 값이 반복된다.
- 아날로그가 이미 하는 방식(수집 주기와 송신 주기 분리)을 I2C 에도 적용하는 것.
- 구현 지점은 한 곳: `tick_i2c()` 의 "같은 t_ms 면 건너뛰기" 가드를
  송신 주기 판정으로 바꾼다. 캐시 변수(`mk_i2c_last`)는 이미 있다.

### 짚어둔 것

- 레코드의 `t` 는 획득 시각(설계 원칙 2)이므로 반복 송신 줄들은 **같은 `t`**
  를 달고 나간다. 클라우드 dedupe 가 (device_id, t, type) 기준이면 서버에서
  한 건으로 접힌다 — "끊김 없는 스트림" 이 목적이면 의도대로다.

## 같은 날 사용자 메시지에서 온 미해결 항목 (돌아와서 처리 예정)

1. 송신 주기 재조정 — 현재 아날로그 10ms, GNSS 즉시(~50ms), IMU 100ms.
2. GUI 수신 NDJSON 형식 ≠ 젯슨 수신 형식 — 통일 예정. 그때까지는 젯슨
   실캡쳐 형식 8종(flow1/flow2/pressure_paint/temp_air/humidity/gnss/imu/
   valve, schema_ver 1, device_id "1")이 기준.
3. IMU 는 UM981 쪽 10Hz 상한으로 보임.
   → **확인 결과 (2026-08-30): UM981 제약이 아니다.** RAWIMUX 는 100 Hz 까지
   가능(UM981 매뉴얼 p.20 §2.3.5, 설계 §4.8 인용). 10 Hz 는 펌웨어가
   보수적으로 시작한 값이다 — mk_gnssctl.c:77 `RAWIMUXA 0.1` 한 줄.
   올리려면 이 값 + 링크 대역폭·본선 파서 부담 계산(설계 §4.8 의 조건).
4. GNSS 를 더 빠르게 보낼 수 있는지 설정 확인 예정.
   → **확인 결과 (2026-08-30): 이미 실측 상한으로 돌고 있다.** ONTIME
   0.05(20 Hz = 50ms)가 UM981 실측 상한(mk_gnssctl.c:63, 2026-08-20 실기기
   에서 1→0.2→0.1→0.05→0.02 차례 시험). **0.02(50 Hz)를 요구하면 오히려
   10 Hz 로 떨어진다** — 더 못 올린다.
5. ~~"오늘(현장에서) 발견된 문제들"~~ — **별도 목록이 아니다** (사용자 확인
   2026-08-30). 위 1~4 가 전부다. 따라서 이 메시지의 미해결 항목은 모두
   방향 확정 또는 확인 완료: 1 → 결정 1(수집·송신 분리), 2 → 결정 2(형식
   통일), 3·4 → 확인 결과 기재됨.

---

# 결정 2 — USB(본선)도 젯슨 형식으로 통일 (사용자 결정, 2026-08-30)

> 위 2번(형식 불일치)의 해소 방향 확정. 역시 아직 코드 변경 단계 아님.

**USB(USART3, GUI)로 나가는 텔레메트리를 규격 v3 레코드 대신 젯슨(Cloud
v1.7) 형식으로 바꾼다.** 직렬화기 한 벌(mk_cloud)이 두 링크(USART2·USART3)
에 같은 줄을 준다. `$CFG`/`$HB`/`$STAT` 등 `$` 제어 줄은 레코드가 아니므로
그대로.

## v3 에만 있던 것 4가지의 처리 (전부 논의 완료)

| v3 필드/동작 | 결정 |
|---|---|
| `seq` (유실 검출) | **젯슨 형식에 선택 필드로 추가** — 계약 §15 허용 범위. 유실 집계 유지 |
| `status` (단선 의심 등) | **버린다** — "안 오면 문제" 로 충분(사용자 판단). LED3 판정은 펌웨어 내부라 무관. 아날로그 단선은 `ma` 값으로 GUI 가 유도 가능 |
| `connector_id` (채널 식별) | **채널 필드 `ch` 를 선택 필드로 추가** — 아래 상세 |
| fix 없는 gnss | **비문제로 확인** — 발행 조건은 RTK 가 아니라 "위치가 문장에 있는가"(mk_cloud.c:381 have_* 검사). 단독측위(fix=1)부터 나간다. 침묵은 위치 자체가 없는 콜드스타트·실내뿐이고, 그때 위성 수는 미니 디스플레이·LED2 가 보드에서 직접 보여준다 |

## 채널 식별 — `ch` 필드 대신 **카탈로그 역매핑** (사용자 결정, 재론 후 확정)

처음에는 레코드에 `ch`(커넥터/포트 번호) 선택 필드를 넣기로 했으나,
**PC 가 이미 보드 파라미터를 통째로 갖고 있다**($CFG,LIST)는 사용자 지적으로
방향 변경 — 레코드는 젯슨 캡쳐 형식 그대로, 한 필드도 안 붙인다.

- **역매핑**: GUI 가 카탈로그의 `ainN.cloud` 문자열을 거꾸로 읽어
  레코드 `type` → 채널 대응을 만든다. 영점·실시간 mA·스트림 트리가 이것으로
  채널에 붙는다. 이름 변경($CFG,SET)은 GUI 자신이 보내므로 즉시 갱신 가능.
- **중복 검사**: 같은 문자열이 두 채널에 있으면 GUI 가 카탈로그에서 알아채고
  설정 화면에 경고("flow1 이 J3·J4 에 중복 — 구분 불가"). 펌웨어 강제 없음.
- **남는 한계 하나**: 같은 종류 I2C 센서 2대(온습도 2대 등)는 이름 자체가
  없어(종류에서 자동 유도) 가를 수 없다 — GUI 는 "동종 2대 — 구분 불가"
  경고까지만. 현재 구성은 종류당 1대라 실문제 없음. 필요해지는 날 `ch` 를
  선택 필드로 재도입하면 계약 안 깨고 해결된다.
- **비용**: I2C 종류→클라우드 타입 표(온습도→temp_air+humidity)가 펌웨어
  `mk_cloud.c` 와 호스트에 한 벌씩 생긴다 — 기존 이중 정의(screen.py ↔
  mk_i2c.c) 옆에 붙이고 CLAUDE.md 주의 항목에 추가할 것.

## 결정 3 — 보드 파라미터를 PC 파일로 상시 보관 (사용자 결정, 2026-08-30)

지금은 사본이 손 관리다 — `tools/restore_board_config.py` 의 PLAN 리스트에
하드코딩("실물 카탈로그 덤프에서 옮겼다"). 이것을 자동화한다:

- **접속 시** `$CFG,LIST` 전체를 PC 파일로 저장, **`$CFG,SET` 성공 시마다 갱신**
- 쓰임 ① 역매핑·중복검사의 소스(보드 미접속 상태에서 로그 해석 포함)
  ② 굽기 후 복원 — restore_board_config.py 가 PLAN 대신 이 파일을 읽는다
  ③ 설정 백업 — 플래시가 날아가도 마지막 상태가 PC 에 남는다

## 이번엔 GUI 변경이 실제로 있다

카탈로그 자동 렌더링과 무관한 영역이다 — 레코드 **파싱** 층이 바뀐다:

- `host/core/records.py` 파서를 젯슨 형식 기준으로 재작성
- 스트림 트리·실시간 mA·영점은 카탈로그 역매핑으로 채널 대응,
  진단 탭 유실 집계는 `seq` 로 유지
- `link_usage`/`field_budget`(전송 탭 사용량 어림)도 레코드 모양 기준이라
  재작성 대상 (세션 초반에 "무관" 이라 했던 것은 통일 전 기준 — 정정)
- 879개 시험 + `fake_board.py` 가 따라간다
- 단선 의심 표시는 status 폐기에 따라 "N초 무수신 = 문제" + ma 기반으로 단순화

---

# 검토 (2026-08-30, 결정 확정 후 총검토)

결정 1·2·3 상호 모순 없음. 구현 전에 반드시 다룰 지점 8개:

1. ✅ **[확정 2026-08-30] ain 큐 드레인을 mk_cloud 가 상속한다.**
   mk_telem 은 채널별 **표본 큐**를 드레인한다(슈퍼루프가 한눈파는 동안
   표본이 큐에 밀렸다가 전부 나감 — peak 4 실측, drops 0). mk_cloud 는
   병렬 소비자라 큐를 못 쓰고 `mk_ads_last`(마지막 값 한 칸, 덮어쓰기)만
   읽는다(mk_cloud.c:239) — 루프가 10ms 이상 지연되면 표본이 덮여
   **세어지지도 않고** 사라질 수 있다. 통일로 mk_telem 이 은퇴하면 큐
   소비권이 비므로 mk_cloud 의 tick_ain 을 큐 드레인으로 바꾼다 —
   라운드로빈·기아 방지(`688ce00` 수정분)까지 mk_telem 에서 통째로 이식.
   **포맷 변경 없음** — 표본을 어디서 꺼내는지만 바뀐다. ain 만 해당
   (i2c 는 큐 불필요 구조 + 결정 1 의 캐시 반복, gnss 는 자체 원시 큐).
   링 포화 시 줄 단위 드랍(0.015% 성질)은 큐와 별개로 그대로 —
   그것은 seq 구멍으로 세어진다.
2. ✅ **[확정 2026-08-30] 침묵 게이트 유지 — 자리만 옮긴다.** 현재 게이트는
   mk_telem 에 붙어 있다(main.c:828). 통일 후 같은 줄을 두 링에 넣되
   **USB 쪽 emit 만 HB 신선도로 게이트**, 젯슨은 항상. "빼면?" 을 검토했으나
   실증 사고 2건(F103 굳음 — 전원 차단으로만 복구 / COM23 트래픽 중 굽기
   실패)의 재발 방지책이라 유지로 확정. HB 는 CONFIG 모드 열쇠이기도 해서
   어차피 못 뺀다(USB 감지 하드웨어 없음).
3. ✅ **[확정 2026-08-30] 반복 주기는 두 링크 공통이 맞다** (사용자 확정).
   직렬화기가 한 벌이므로 주기 손잡이 하나가 USB·젯슨 양쪽 도착 간격을
   같이 바꾼다. 서버 페이싱은 젯슨/서버 몫(기존 원칙 그대로).
4. ✅ **[확정 2026-08-30] 대역폭은 기존 사용량 표시기가 지킨다.**
   `link_usage.py` 가 이미 실제 줄을 만들어 재고 젯슨 링크(921600 고정)를
   따로 계산한다 — 별도 하한 강제 없이 **기본값 200ms + 화면 % 표시**로
   충분(사용자 확인: "각 링크를 켜면 주기에 따라 사용량이 나온다").
   재작성 시 바뀌는 것 3개: ① 표본 줄을 젯슨 형식(+seq)으로 ② 통일 후
   모든 종류가 두 링크에 나가므로 같은 트래픽을 젯슨 921600·USB 2M 두
   한도에 각각 댄다(빡빡한 쪽은 항상 젯슨) ③ i2c 줄 수는 수집 주기가 아니라
   반복 송신 주기로. 참고 계산(2026-08-30): 현 배선(3ch×10ms+온습도+GNSS
   20Hz+IMU 10Hz+반복 100ms) ≈ 47KB/s = 젯슨 51%. 최악(7ch×10ms)은 seq
   포함 시 921600 초과 — 2M 승격은 Q2 의 직결 2Mbps 2% 유실 전력 때문에
   실측 후 결정.
5. ✅ **[확정 2026-08-30] din 도 타입 문자열을 사용자가 정한다** —
   `dinN.cloud` 자유 문자열(ain 과 같은 원칙). J20 에 "valve" 를 치면 지금
   젯슨 수신분과 동일한 레코드, 빈 값(기본) = 미발행. 레코드 모양은 전부
   valve 형(`state` 0/1) — 타입 이름만 갈린다. 단 gnss 태깅용 밸브 지정
   (`cloud.valve`, 디지털 입력 OR)은 역할이 달라(레코드 이름 vs 태깅 소스)
   그대로 둔다.
6. ✅ **[확정 2026-08-31] gnss.echo 는 `$` 진단 줄로 이전하여 유지** —
   레코드(`type:"gnss_raw"`)가 아니라 `$GNSSRAW,<원문>` 류의 `$` 줄로
   내보낸다. `$` 줄은 통일 대상 밖이라 원칙이 안 깨지고, 젯슨 수신기는
   `$` 줄을 원래 버린다. UM981 명령 응답·원문 확인의 유일한 GDB 없는
   수단이라 버리지 않는다(GDB 는 붙이면 리셋되는 함정). GUI 는 `$` 처리부에
   한 갈래 추가. 규격 §7.7 은 `$` 줄 절로 옮겨 개정.
7. **문서 개정 대상**: ① protocol/specification.md 본선 텔레메트리 절
   ② 계약 v1.7 에 `seq` 선택 필드 추가 개정 ③ 2026-08-21 설계 문서의
   "본선은 v3 그대로"(§2)·"송신 주기 두지 않는다"(§4.7) 두 항목이 이번
   결정으로 **폐기**됨을 명시 — 이력 충돌 방지.
8. ✅ **[확정 2026-08-31] 포트별 두 손잡이** (사용자 결정 — "각 포트별,
   수집 주기와 송신 주기를 입력"):
   - **수집 주기** = `i2cN.period_ms` (기존 항목 그대로). 하한은 드라이버가
     데이터시트 근거로 보유(AM2320 2000ms / MLX90614 250ms / BH1750 180ms)
     — 하한은 펌웨어의 취향이 아니라 **센서(종류)를 따라다니는 값**이다.
     새 센서 모델 = 새 종류(드라이버) = 그 센서의 하한이 함께 온다. 같은
     모델의 주소 변경은 지금도 `addr` 만 바꾸면 된다 (사용자 합의 2026-08-31).
   - 🔴 **조용한 클램프 폐지** (2026-08-31 합의): 지금은 하한 미만을 설정해도
     말없이 실효 주기로 동작해 화면 설정값이 거짓말한다(설계 원칙 4 위반).
     → 하한 미만 SET 은 **사유와 함께 거절**한다("AM2320 변환 하한 2000ms")
     — GUI 는 거절 사유를 이미 그 자리에 띄우는 구조가 있다.
   - **송신 주기** = `i2cN.tx_period_ms` (신설, 포트별) — 캐시 최신값을
     이 주기마다 반복 송신(결정 1). 기본 200ms 안. 카탈로그 note 에
     "짧으면 젯슨 링크 포화 가능" — 실제 가드는 GUI 사용량 표시(4번).
   - GUI 코드 변경 없음 — 기존 i2c 그룹에 항목 추가라 저절로 그려진다.

부기: 이 결정들은 feat/rtcm 브랜치 작업(RTCM 수신·NTRIP 포워더) 위에
얹힌다 — RTCM 은 레코드가 아니라 수신 경로라 형식 통일과 직교한다.

---

# ✅ 해결 — 온습도(AM2320) 읽기 실패 (2026-08-31 진단 세션, 같은 날 해결)

**최종 원인과 해결** (아래 진단 기록은 경과 그대로 보존):

1. **1차 원인 — 설정 중복**: i2c12·i2c13 이 같은 버스(I2C5)에서 같은 주소
   92 를 동시 폴링 (현장에서 센서를 J13→J12 로 옮기며 생긴 잔재).
2. **2차 원인 — 그 여파로 STM32 I2C5 주변장치가 오류 상태에 굳음**: 센서를
   뽑아도 status=1(응답 없음)이 아니라 **status=2 가 나왔다** — bsp 의
   map_rc 가 "주소 NACK 외 모든 HAL 오류" 를 2 로 접기 때문(mk_i2c_io.c:97).
   빈 버스에서 2 = 주변장치/버스가 굳었다는 신호다. **센서·케이블·커넥터
   교체 시험은 이 상태에서 전부 무효였다.**
3. **해결**: 중복 제거($CFG,SET) → SAVE → **보드 전원 재투입**(주변장치
   리셋) → 빈 버스에서 status=1 정상 확인 → 센서 연결 → **25.4°C /
   40.4% status=0 정상 수신**. i2c12 켜짐 상태로 재 SAVE 완료.

🔴 **교훈 (다음에 또 만나면)**: I2C 진단에서 **"센서를 뽑았는데 status=2"
는 센서 문제가 아니라 보드 주변장치 굳음**이다 — 부품을 갈지 말고 전원을
재투입한다. 그리고 같은 버스 짝 커넥터(J12/J13 등)에 같은 주소를 두 포트로
켜 두면 이 굳음을 유발할 수 있다 — GUI 중복 경고(결정 2 보완)가 생기면
설정 단계에서 잡힌다.

## 이하 당시 진단 경과 (원본 보존)

**증상**: GUI 에 온습도만 안 옴(다른 값 정상), LED3 빨강 점멸.

**확정된 사실** (실물 보드 조회·수신으로 확인):

1. **설정 오류가 실재했다** — i2c12·i2c13 이 **둘 다 켜진 채 주소 92·종류
   2(온습도) 중복**이었다(i2c13 이름만 "적외온도"). 같은 버스(I2C5)에서 한
   센서를 두 포트가 두드리는 상태 → AM2320 최소 읽기 간격 2초 위반.
   현장에서 센서를 J13→J12 로 옮기며 i2c12 를 새로 켰는데 i2c13 이 옛 온습도
   설정을 문 채 남은 것으로 보임. **그러나 이걸 해소해도 안 읽힘 — 근본
   원인 아님.**
2. 주소 92(0x5C)는 정상 — 드라이버 기본값과 일치, AM2320 고정 주소.
3. **센서가 응답은 한다. 데이터가 깨진다** — 레코드가 status=2(데이터
   오류, CRC 류)로 옴. status=1(응답 없음)이 아니다 → 단선이 아니라
   신호 품질/센서 내부 문제 계열.
4. 센서 전원 리셋(뽑았다 꽂기) 무효. J12·J13 어느 쪽도 무효.

**남은 용의자**: ① 케이블 손상(이동 중 — "응답은 되는데 비트가 깨짐"의
전형) ② 센서 자체 손상 ③ 보드 I2C5 버스(가능성 낮음 — 8/19 에 같은
버스에서 MLX+AM2320 동시 동작 실증).

**다음 시험**: MLX90614(적외온도)를 J12 에 꽂아 읽히는지 — 읽히면 보드
정상 확정, 온습도 케이블→센서 순으로 교체 시험.

🔴 **보드 현재 상태**: i2c12·i2c13 enabled 를 **RAM 에서만** 껐다(센서
분리 상태라 오경보 방지). **플래시에는 중복 설정이 그대로 남아 있어
전원 재투입 시 되살아난다** — SAVE 여부는 사용자 결정 대기.
